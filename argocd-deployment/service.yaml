apiVersion: v1
kind: Service
metadata:
  name: juiceshop-app
  labels:
    app: juiceshop-app
spec:
  ports:
  - name: http
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: juiceshop-app
  type: LoadBalancer
